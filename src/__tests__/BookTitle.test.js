import { render, screen } from "react-dom"
import { BookTitle } from "../components/BookTitle"




    it("sanity check", () => {
        expect(2 + 2).toBe(4)
    })

    // it("renders book title", () => {
    //     //render(<BookTitle />)
    //     //expect(screen.getByTestId("bookTitleTest")).toBeInTheDocument()
    // })
